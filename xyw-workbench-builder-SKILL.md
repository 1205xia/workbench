# 移动端个人工作台生成 Skill

> 整理日期：2026 年 9 月 2 日

## 适用场景

当客户提出“做一个工作台”“做一个手机 App”“记录每日任务”“学习单词”“记账”“把个人资料放进一个 App”或类似需求时，使用本 Skill。

目标是生成一个真正可以安装到 Android 手机的个人工作台，而不是只做一个桌面网页。默认技术方向可以使用 React + Vite + Capacitor + Android，但实现方式可以根据客户设备和现有项目调整。

## 最重要的执行规则：先确认，再开发

读取本 Skill 后，不要立即写代码、下载词库、生成 APK 或修改客户数据库。必须先向客户提问并等待确认。

提问要用客户能直接回答的中文，避免一次问太多技术细节。至少确认以下内容：

1. 左侧栏或主导航需要哪些模块？每个模块要解决什么工作内容？
2. 这个工作台的主要目的是什么？例如求职管理、学习计划、个人生活管理、项目管理或综合使用。
3. 是否需要记账？如果需要，是否需要收入、支出、月度统计、余额、补账和自定义分类？
4. 是否需要英语学习？学习英语四级、六级，还是自定义词库？
5. 如果学习四级或六级，每天需要多少单词、多少短语？是否需要音标、中文释义、英文例句、例句翻译、默写和复盘？
6. 是否需要批量导入每日任务？客户会粘贴什么格式的文本？是否允许导入内容覆盖旧任务？
7. 任务完成后是否自动跳转到下一天？是否需要左右滑动切换日期？
8. 工作台只做 Android APK，还是还需要电脑网页、平板版本或 iOS 版本？
9. 客户偏好的视觉风格、颜色、字体、圆角程度、信息密度和参考 App 是什么？
10. 是否允许使用客户提供的数据库、简历、项目文件或个人照片？哪些内容不能读取或不能写入？
11. APK 是否需要签名、应用名称、图标、启动页和版本号？客户是否接受测试版 APK？
12. 客户希望先看可交互预览，还是直接生成安装包？默认必须先给预览，客户确认后再打包。

如果客户的回答不完整，继续追问影响产品结构的问题。不要把不确定的内容擅自做成固定功能。

## 已确认过的一种工作台参考方案

以下内容是一次具体客户需求中已经确认的方向，只能作为参考模板。新的客户仍然需要重新确认，不得默认套用全部个人信息。

### 产品形态

- 目标设备是 Android 手机。
- 交付物是可以安装的 APK。
- 手机不依赖电脑开机即可使用。
- 在生成 APK 前先提供可交互预览页面。
- 预览页面每次完成重要修改后都要重新打开并发给客户。
- 最终 APK 放到客户指定的 D 盘位置。
- 项目源码、必要配置和可维护文件保留；只删除明确的临时文件、旧安装包和无关垃圾。

### 数据隔离

- 客户提供的“XYW 数据库”可能包含简历、岗位、面试 Skill 和个人材料。
- 工作台项目应放在独立项目目录，不要把应用缓存、词库、任务、记账数据写回原始 XYW 数据库。
- 只有客户明确授权的内容才能读取。
- 个人简介可以从客户资料中提炼，但要让客户知道使用了哪些信息。
- 不要把个人简历、照片、联系方式、财务记录或其他敏感信息上传到第三方服务。
- 不要把 API Key、密码、Token 或个人隐私写进 Skill、源代码或日志。

## 参考导航结构

一次已确认的初始导航为：

1. 个人主页
2. 每日单词速记
3. 每日任务
4. 记账

导航应适合手机使用。小屏幕可以使用抽屉式左侧栏、底部导航或按钮打开的侧栏，但必须让用户一进入就知道如何新增任务、学习单词和记一笔账。

## 视觉设计方向

默认参考方向是年轻、小清新、轻量、耐看，避免“AI 模板感”和老旧办公软件风格。

- 优先考虑马卡龙色系或低饱和明亮色。
- 黄色不是硬性要求；如果黄色刺眼，应改成柔和的蓝、粉、紫、薄荷色等组合。
- 不默认使用大面积纯黄背景。
- 字体要清晰、适合手机阅读；除非客户明确要求，不要强制使用宋体。
- 页面不要堆叠大量大卡片、渐变、装饰性光球或夸张标题。
- 控件要有明确反馈，按钮点击后必须出现状态变化、提示、进度变化或页面跳转。
- 文字必须在手机屏幕内正常换行，不得被按钮、卡片或导航遮挡。
- 先做实际工作界面，不要先做营销式 Landing Page。
- 图标使用现有图标库时优先使用图标库，不要为简单按钮手绘复杂 SVG。

## 每日任务模块

### 展示与完成

- 未完成任务显示为白色或浅色。
- 任务前面有小圆圈。
- 点击圆圈后变成对号，任务显示已完成。
- 每个工作内容必须是独立任务。例如“数据整理”“数据分析”“做表格”必须是三个任务，不能合并成一句。
- 支持新增、编辑、删除任务。
- 日常事务也可以作为任务，例如拿快递、发快递、缴费、复印材料等。
- 当某一天的全部任务完成后，默认自动进入下一天；同时提供手动切换日期的方式。
- 如果导入内容包含多个日期，任务页面要按天查看，而不是把所有日期的任务堆在一个页面上。
- 移动端可以使用左右滑动切换日期：向右进入下一天，向左进入上一天。方向必须在预览中实际测试。

### 一键批量导入

入口尽量只保留一个“一键导入”按钮。

输入方式：

- 支持粘贴一整段“日期 + 任务”文本。
- 日期可以识别 `2026-09-02`、`9月2日`、`2026年9月2日` 等常见格式。
- 任务通常由 Markdown 列表、短横线、编号或换行分隔。
- `目标`、`当天目标`、阶段标题、说明文字不能误识别成普通任务。
- 反斜杠、Markdown 粗体符号和多余空格应清理。
- 一句话中由多个独立动作组成的内容，要尽可能拆成独立任务；不应简单按逗号机械切分。

覆盖策略必须在执行导入前明确告诉客户。推荐策略：

- 只覆盖导入文本中明确出现的日期。
- 导入日期早于客户当前保留进度的任务时，不重置已经完成的任务。
- 已完成日期可以保留原完成状态；未完成日期可以按客户选择覆盖或合并。
- 导入后给出预览摘要：识别到多少日期、多少任务、哪些日期将被新增或更新。
- 默认不删除未出现在本次导入文本中的日期。

## 每日单词速记模块

### 词库选择

开发前必须向客户确认：

- 使用英语四级、六级，还是其他词库。
- 是否使用完整词库，还是只使用高频筛选词。
- 是否排除客户已经掌握的基础词，例如 `and`、`are`、`in`、`for`、`on` 等。
- 每天学习多少单词和短语。

如果客户要求“高频”，AI 可以搜索公开资料并进行筛选，但要说明排序依据，避免把来源不明的词频当成绝对考试排名。词库数据应在本地打包，离线也能使用。

### 每日内容

一个单词至少包含：

- 单词
- 音标
- 中文释义
- 英文例句
- 例句中文翻译
- 可选：词性、常见搭配、短语

如果例句或翻译缺失，不要静默显示空白。应补充可靠数据，或明确显示“暂无例句”，并在交付前告诉客户。

### 学习状态

单词需要有明确的状态：

- 不认识：保留在当前学习批次中，并给出反馈。
- 认识：表示见过并记住了，可以快速略过，不要求默写。
- 已默写：输入答案后检查，给出正确、错误或接近正确的反馈。
- 已记住：单词卡片变为灰色或低强调状态，并计入进度。

点击任何状态按钮后必须有明显反馈，例如按钮样式变化、状态文字、进度数字或提示语。不能出现“点击了但看起来没反应”。

### 自动刷新和手动刷新

- 每天首次打开时，自动显示当天的新 20 个单词（数量以客户确认值为准）。
- 如果前一天没有学习完，必须明确采用“继续未完成批次”还是“自动换新批次”，并提前告诉客户。
- 客户当天学完一批后，可以点击“刷新下一轮”继续学习后面的新词。
- 刷新不能重复显示上一批单词。
- 刷新游标必须持久化保存，退出 App 后仍然有效。
- 第二天打开时，应根据保存的游标和学习记录自动更新。
- 旧缓存损坏、空数组、无效游标或版本升级时，必须自动修复并至少生成一批有效单词。
- 页面不能出现单词数量为 `0/0` 但短语正常显示的情况。

### 复盘

当当前批次全部完成后，底部显示“全部记住，开始复盘”。

复盘要求：

- 不要强制把 20 个单词和 20 个选项做成一一对应的大页面。
- 每个单词使用一个紧凑的三选一界面。
- 三个中文翻译选项应随机排序，不能总是第一个单词对应第一个选项。
- 选中后立即显示答对或答错反馈。
- 可以关闭复盘，不应强制用户选完才能退出。
- 复盘完成后必须有结束页或结束按钮。
- 结束页提供两个选项：
  - “今日任务已结束”
  - “继续刷新下一轮”
- 继续刷新后应真正更换到下一批单词。

## 记账模块

### 月度模型

- 记账应按月份查看，而不是把所有日期混在一起。
- 页面显示当前月份。
- 支持切换上个月、下个月，或选择月份。
- 显示本月收入、本月支出和本月可用余额。
- 余额计算：本月收入减去本月支出。
- 每个月要有支出总和，不能只显示单笔记录。

### 收入与生活费

- 客户可以手动设置本月生活费或收入。
- 点击确定后，输入框消失，顶部显示“本月生活费收入”和具体金额。
- 固定后的生活费不应在普通记账表单中继续显示为可编辑输入，避免误触。
- 点击顶部的“本月生活费收入”区域后进入编辑状态。
- 编辑状态提供“取消”按钮；取消不应修改原金额。
- 修改并确定后，更新本月固定金额。
- 不得擅自使用测试金额作为客户真实生活费。

### 记账录入

- 类型至少包括收入和支出。
- 日期使用易懂的月份和日选择器；优先让用户从当月 1 日到月底的下拉列表选择，不强迫手动输入。
- 分类至少可以包含：餐饮、衣物、出行、居住、日用、学习、医疗、娱乐、人情、其他。
- 提供“添加”分类，允许客户创建自定义分类。
- 备注不是必填项；如果客户不需要，可以隐藏。
- 输入金额后点击“记一笔”保存。
- 支持补账：客户可以在第二天选择昨天或其他日期后补录。
- 每笔记录可以查看和删除，但删除应有明确反馈。

## 个人主页模块

参考功能：

- 修改头像。
- 修改昵称。
- 填写出生日期。
- 填写求职方向。
- 提供简短个人介绍。

个人介绍可以根据客户授权的简历、岗位和项目资料粗略生成，重点是方便客户在手机上快速向别人介绍自己。不要擅自添加未经确认的学校、公司、联系方式、成绩或经历。

头像更换必须在真实手机或预览中测试文件选择、读取和显示效果。

## 工程与验证流程

### 开发顺序

1. 读取现有项目结构和客户提供的资料边界。
2. 先完成需求确认，不把不确定点写死。
3. 先实现核心数据模型和持久化。
4. 实现手机端主要页面。
5. 启动本地预览服务。
6. 用真实点击、输入、刷新、切换日期和返回操作测试。
7. 把预览页面发给客户确认。
8. 根据客户反馈修复。
9. 客户确认后再构建 APK。
10. 检查 APK、构建日志和输出目录，清理临时垃圾文件。

### 必测验收清单

- 首次打开每日单词页能显示客户要求数量的真实单词。
- 每个单词都有例句和中文翻译，或明确说明缺失原因。
- 单词状态按钮有反馈。
- 默写答案正确和错误时有反馈。
- 刷新下一轮确实换词且不重复上一批。
- 关闭并重新打开后，学习状态和刷新游标仍然存在。
- 任务导入能按日期和独立动作拆分。
- 全部完成任务后自动进入下一天。
- 任务页面支持左右滑动时，实际在手机尺寸下测试。
- 记账可选择任意当月日期，能补录历史日期。
- 收入、支出、余额和月总和计算正确。
- 生活费确定后输入项消失，点击顶部金额可以重新编辑并取消。
- 头像可以更换。
- 手机窄屏无横向溢出、按钮重叠或文字遮挡。
- APK 安装包可以生成，且使用的是最终确认后的代码。

## 文件和清理规则

- 客户指定的安装包、导出数据和文档放到客户指定盘符，默认可使用 `D:\`。
- 不要把临时截图、调试日志、旧 APK、缓存压缩包和测试导出文件混在最终交付目录。
- 清理前先列出拟删除文件并确认它们不是源码、数据库、配置或客户资料。
- 不删除原始 XYW 数据库，不删除客户未授权的文件。
- 最终交付时明确列出保留的安装包路径、预览地址和测试状态。

## 沟通方式

- 发现问题时直接说明具体原因，不要只说“下一版会修复”。
- 每次修改前告诉客户准备改什么。
- 每次重要修改后主动发预览页面。
- 不要把网页预览说成已经安装到手机；网页预览和 Android APK 是两个交付物。
- 不要在客户没有确认时提前生成最终 APK。
- 客户要求“先回答问题”时，先回答，不要直接开始操作。
